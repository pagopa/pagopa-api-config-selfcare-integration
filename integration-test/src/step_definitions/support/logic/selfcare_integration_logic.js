const assert = require("assert");
const {
    readBroker,
    readCIStationAssociation,
    readCreditorInstitution,
} = require("../clients/api_config_client");
const { debugLog } = require("../utility/helpers");


async function assertStationIncludedInResponse() {
    console.log(` - ..`);

    debugLog(``);
}

async function checkNonExistingBroker() {
    console.log(` - ..`);

    debugLog(``);
}

async function checkNonExistingCreditorInstitution() {
    console.log(` - ..`);

    debugLog(``);
}


async function retrieveBroker() {
    console.log(` - ..`);

    debugLog(``);
}


async function retrieveCreditorInstitution() {
    console.log(` - ..`);

    debugLog(``);
}

async function retrieveStationsByBroker() {
    console.log(` - ..`);

    debugLog(``);
}

async function retrieveStationsByCreditorInstitution() {
    console.log(` - ..`);

    debugLog(``);
}

